package com.example.finalprojectactual;
public class quizzer extends MainActivity {
    /**
     * Constructor takes XML as argument.
     * Gets called for every individual question.
     * Extracts Question, answers, which one is correct.
     * Function isCorrect takes an answer string returns whether or not answer is correct.
     * might have to be "static" but we'll find out
     */
}
